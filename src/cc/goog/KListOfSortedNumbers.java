package cc.goog;

public class KListOfSortedNumbers {
	/*
	public static void main(String[] args) {
		int[] list1 = {4, 10, 15, 24, 26};
		int[] list2 = {0, 9, 12, 20}; 
		int[] list3 = {5, 18, 22, 30};
		ListItem[] flatList = kmerge(new int[][]{list1, list2, list3});
		
		// Flatten all the list by doing n way merge.
		ListItem[] flatList = new ListItem[list1.length + list2.length + list3.length];
		int list1Index = 0, list2Index = 0, list3Index = 0; 
		
		for (int i = 0; i < flatList.length; i++) {
			//flatList[i] = new ListItem();
			// Find the lowest number in 3 list.
			// First do the bound check
			if (list1.length > list1Index && list2.length > list2Index && list3.length > list3Index) {
				if (list1)
			}
		}
	}
	
	private static LinkedList<ListItem> kmerge(int[][] kElem) {
		int[] kIndexCounter = new int[kElem.length];
		
		ListItem 
		return null;
	}

	static class ListItem {
		int number;
		int originKIndex;
	}
	static class List1Item extends ListItem{}
	static class List2Item extends ListItem{}
	static class List3Item extends ListItem{}
	*/
}
